package darwinsquest.core;

/** 
 * Interface that represents a {@link Move} that can change the stats of a {@link Banion}.
*/
public interface StatsMove extends Move {
    
    /*Statistic computeDelta(Statistic stat);
     * This method will be private in a implementation of this interface.
    */
}
